var countries_rows_template = '<% _.mixin({    chunk : function (array, unit) {        if (!_.isArray(array)) return array;        unit = Math.abs(unit);        var results = [],        length = Math.ceil(array.length / unit);         for (var i = 0; i < length; i++) {            results.push(array.slice( i * unit, (i + 1) * unit));        }        return results;    },    active_country : function (country, active) {      var active_country = jQuery("#current_country a").text();      if (active_country == country) {        return active;      }      return "";    },    active_continent : function(continents, continent, active) {          if (selectedContinent -1 == jQuery(continents).index(continent)) {              return active;      }      return "";    },    country_list_class : function(list) {      if (list.length > 1) { return "country-list" } else return  "single-country-list";     }}); %><% _.each( continents, function( continent ){ %><div class="DropdownDivColumn DropdownDivColumn2 <%= _.active_continent(continents, continent, "active") %>">   <h2 ><%= continent.title %></h2>  <ul>    <% li_group =  _.chunk(continent.countries, 16) %>      <% _.each( li_group, function( lig ){ %>         <div class="<%= _.country_list_class(li_group) %>">          <% _.each( lig, function( li){ %>             <li <%= _.active_country(li[0], "class=active") %>><a href="<%= li[1]%>"><%= li[0] %></a></li>          <% }); %>        </div>                 <% }); %>  </ul></div><% }); %><div class="DropdownDivColumn2"><div class="country-count"></div></div><div class="DropdownDivColumn2"><div class="global-image"><div><div class="word-map">Visit our<br><a href="/global/home">Global website</a></div></div></div></div>'